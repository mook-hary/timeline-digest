{
  "name": "timeline-digest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multi-source ingest and digest layer. Consumes public feeds; optional OpenAI judge on candidate pairs only.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "ingest:x": "node src/ingest-x.js",
    "ingest:web": "node src/ingest-web.js",
    "unify": "node src/unify.js",
    "cluster": "node src/cluster.js",
    "semantic": "node src/semantic.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "fast-xml-parser": "^5.11.1",
    "openai": "^6.49.0"
  }
}
